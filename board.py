from PyQt5.QtWidgets import QMainWindow, QFrame

# we want a pause, play, hold/swap, next shape, score, 

class tetris(QMainWindow)

    def __init__(self):
        super().__init__()
        self.initUI()

    def initUI(self):
        self.gameboard = board(self)

class board(QFrame):
    boardWidth = 10
    boardHeight = 22
    tickRate = 500

    def __init__(self, parent):
        super().__init__(parent)
        self.initBoard()
    
    def initBoard(self):
        self.


    def tick

    def spawnShape()

    

        


